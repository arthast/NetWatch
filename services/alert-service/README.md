# alert-service

`alert-service` is the internal NetWatch service responsible for alert
lifecycle and alert storage. It exposes gRPC APIs for listing alerts and for
processing check result transitions from `monitor-service`.

Use the root [README](../../README.md) for Docker Compose demo commands and the
full architecture overview.
